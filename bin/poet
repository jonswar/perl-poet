#!/usr/bin/env perl
use Poet::Environment::Generator;

my ( $app_dir, $app_name ) = shift(@ARGV) or die "usage: $0 app_dir AppName";
Poet::Environment::Generator->generate_environment_directory(
    root_dir => $app_dir,
    app_name => $app_name
);

__END__

=head1 NAME

poet - Generate a Poet environment

=head1 SYNOPSIS

   poet <app_dir> <AppName>

=head1 DESCRIPTION

Generates a skeleton Poet environment for an app called I<AppName> in the
directory I<app_dir>.

=head1 SEE ALSO

Poet

=head1 AUTHOR

Jonathan Swartz

=cut
